@model task_management_system.Models.ViewModels.ProjectTask.ProjectTaskCreateViewModel
@using task_management_system.Models.Enum

<div class="container py-4">
    <div class="row">
        <div class="col-md-7 mx-auto">
            <div class="card border-0 shadow-sm rounded-3 p-4 bg-white">
                <h4 class="fw-bold text-dark mb-1">Cập nhật Công việc</h4>
                <p class="text-muted small mb-3">Thay đổi thông tin, dự án hoặc điều chuyển nhân sự thực hiện task.</p>
                <hr class="opacity-25 mb-4">

                <form asp-action="Edit" asp-route-id="@ViewBag.TaskId" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger small mb-3"></div>

                    <div class="mb-3">
                        <label asp-for="ProjectId" class="form-label fw-medium small text-secondary">Thuộc Dự án</label>
                        <select asp-for="ProjectId" asp-items="Model.ProjectList" class="form-select rounded-2">
                        </select>
                        <span asp-validation-for="ProjectId" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Title" class="form-label fw-medium small text-secondary">Tiêu đề công việc</label>
                        <input asp-for="Title" class="form-control rounded-2" />
                        <span asp-validation-for="Title" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Description" class="form-label fw-medium small text-secondary">Mô tả nội dung</label>
                        <textarea asp-for="Description" class="form-control rounded-2" rows="3"></textarea>
                    </div>

                    <div class="row g-3 mb-4">
                        <div class="col-md-4">
                            <label asp-for="AssigneeId" class="form-label fw-medium small text-secondary">Giao cho nhân viên</label>
                            <select asp-for="AssigneeId" asp-items="Model.UserList" class="form-select rounded-2">
                                <option value="">-- Chưa giao / Để trống --</option>
                            </select>
                        </div>

                        <div class="col-md-4">
                            <label asp-for="Status" class="form-label fw-medium small text-secondary">Trạng thái</label>
                            <select asp-for="Status" asp-items="Html.GetEnumSelectList<ProjectTaskStatus>()" class="form-select rounded-2">
                            </select>
                        </div>

                        <div class="col-md-4">
                            <label asp-for="DueDate" class="form-label fw-medium small text-secondary">Hạn chót (Due Date)</label>
                            <input asp-for="DueDate" class="form-control rounded-2" />
                        </div>
                    </div>

                    <div class="d-flex gap-2 justify-content-end">
                        <a asp-action="Index" class="btn btn-light btn-sm px-3 rounded-2 text-secondary border">Quay lại</a>
                        <button type="submit" class="btn btn-dark btn-sm px-4 rounded-2">Cập nhật thay đổi</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}